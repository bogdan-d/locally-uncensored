<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened Runtime entitlements (referenced via bundle.macOS.entitlements in
  tauri.conf.json; derived from the proven LU-Labs bridge set).

  - allow-jit + allow-unsigned-executable-memory: WKWebView JavaScriptCore JIT.
  - disable-library-validation: whisper/voice sidecar loads Python dylibs not
    signed with our identity.
  - network.client: Ollama/LM-Studio probes, Hugging Face + Civitai downloads.
  - network.server: remote-access bridge listens on port 19191.
  - device.audio-input: microphone for dictation/voice — required in a
    notarized Hardened Runtime build even with NSMicrophoneUsageDescription.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
